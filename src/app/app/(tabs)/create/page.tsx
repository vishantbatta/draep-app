import type { Metadata } from "next";

/** Content is rendered by the (tabs) layout; this only names the URL. */
export const metadata: Metadata = { title: "Create" };

export default function CreateTabPage() {
  return null;
}
